#include "srchALGraph.h"
/*
void main() {
	srchALGraph g;
	g.load("graph.txt");	// 파일로부터 g를 설정함
	printf("그래프(graph.txt)\n");
	g.display();

	printf("DFS ==> ");
	g.resetVisited();	// 모든 정점을 방문하지 않았다고 하고
	g.dfs(0);			// 0번째 정점(A)에서 깊이 우선 탐색 시작
	printf("\n");

	printf("BFS ==> ");
	g.resetVisited();	// 모든 정점을 방문하지 않았다고 하고
	g.bfs(0);			// 0번째 정점(A)에서 깊이 우선 탐색 시작
	printf("\n");
}
*/