osi 7계층

	7 응용
		사용자와 네트워크 간 응용서비스 연결, 데이터 생성
		http, ftp, telnet, smtp/sntp, dns

	6 표현
		데이터 형식 설정, 코드변환, 암/복호화
		jpeg, mpeg

	5 세션
		연결 접속(유지), 동기점
		ssh, tls

	4 전송
		종단간 신뢰성 있고 효율적인 데이터 전송, 슬라이딩 윈도우
		tcp/udp, trcp -> 세그먼트

	3 네트워크
		경로(라우팅) 제공
		ip, icmp, igmp, rip, ospf -> 패킷

	2 데이터 링크 계층
		인접 시스템 간 물리적 연결 이용해 데이터 전송 등
 		hdlc, ppp, llc, ethernet -> 프레임

	1 물리 계층
		매체 간의 전기적, 기능적, 절차적 기능 정의
		rs-232c -> 비트

아(A) 파(P) 서(S) 티(T) 내(NE) 다(DA) 피(PHY) 나다!
	