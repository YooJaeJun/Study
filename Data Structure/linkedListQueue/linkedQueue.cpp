#include "linkedQueue.h"
/*
void main() {
	linkedQueue que;
	for (int i = 1; i < 13; i++) {
		que.enqueue(new node(i));
	}
	que.display();
	delete que.dequeue();
	delete que.dequeue();
	que.display();
}
*/