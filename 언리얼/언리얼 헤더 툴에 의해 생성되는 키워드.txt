"class명".generated.h
	파일에서 감지한 매크로를 기반으로 생성됨

UCLASS()
	클래스가 언리얼의 리플렉션 시스템에 노출될 것임을 나타내는 매크로 중 하나.
	런타임에 오브젝트 속성을 검사하거나 순회 가능
	가비지 컬렉션을 위해 오브젝트에 대한 참고를 관리할 수 있다.

class _API 이름
	프로젝트 모듈의 클래스가 DLL에 제대로 내보내지도록 함으로써
	윈도우 환경에서 프로젝트가 정상적으로 컴파일되도록 도와준다.

GENERATED_BODY()
	기본 UE 타입 시스템에 필요한 자동으로 생성된 기능을 포함하도록 확장된 또 다른 매크로.